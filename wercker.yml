box: wercker/nodejs
build:
  steps:
    - aussiegeek/install-phantomjs
    - script:
        name: chown for npm
        code: sudo chown -R $USER /usr/local
    - npm-install:
        options: -g component mocha-phantomjs
    - script:
        name: make test
        code: make test
